autodock_parameter_version 4.2       # used by autodock to validate parameter set
outlev 1                             # diagnostic output level
intelec                              # calculate internal electrostatics
seed pid time                        # seeds for random generator
ligand_types C HD N OA P             # atoms types in ligand
fld 1ebg_rec.maps.fld                # grid_data_file
map 1ebg_rec.C.map                   # atom-specific affinity map
map 1ebg_rec.HD.map                  # atom-specific affinity map
map 1ebg_rec.N.map                   # atom-specific affinity map
map 1ebg_rec.OA.map                  # atom-specific affinity map
map 1ebg_rec.P.map                   # atom-specific affinity map
elecmap 1ebg_rec.e.map               # electrostatics map
desolvmap 1ebg_rec.d.map             # desolvation map
move 1ebg_lig.pdbqt                  # small molecule
about 17.646 5.470 20.124            # small molecule center
tran0 random                         # initial coordinates/A or random
quaternion0 random                   # initial orientation
dihe0 random                         # initial dihedrals (relative) or random
torsdof 3                            # torsional degrees of freedom
rmstol 2.0                           # cluster_tolerance/A
extnrg 1000.0                        # external grid energy
e0max 0.0 10000                      # max initial energy; max number of retries
ga_pop_size 150                      # number of individuals in population
sw_max_its 300                       # iterations of Solis & Wets local search
sw_max_succ 4                        # consecutive successes before changing rho
sw_max_fail 4                        # consecutive failures before changing rho
sw_rho 1.0                           # size of local search space to sample
sw_lb_rho 0.01                       # lower bound on rho
set_psw1                             # set the above pseudo-Solis & Wets parameters
unbound_model bound                  # state of unbound ligand
do_local_only 50                     # do this many LS runs
analysis                             # perform a ranked cluster analysis
